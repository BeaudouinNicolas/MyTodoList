<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nicolas.beaudouin.mytodolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="nicolas.beaudouin.mytodolist.TodoActivity"
            android:label="@string/mytodolist">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="nicolas.beaudouin.mytodolist.AddTodoActivity"
            android:label="@string/addtodoitem">
            <intent-filter>
                <action android:name="android.intent.action.PICK_ACTIVITY" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
